name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  all-green:
    name: all-green
    runs-on: ubuntu-latest
    steps:
      - name: Baseline repository gate
        run: echo "Repository baseline CI is wired; add repo-specific gates as the repo grows."
